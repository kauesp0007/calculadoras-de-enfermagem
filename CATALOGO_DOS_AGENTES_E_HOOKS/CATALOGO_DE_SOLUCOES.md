# ✅ Catálogo de Soluções Comprovadas (FASE 21)

**Fonte:** `/memories/repo/acervo-solucoes.json` — **6 registros** (padrão de hook, BOM UTF-8, interpolação `${var}`, node `-e` quoting, check-layout/style, validação JSON pós-edição).

## Schema (FASE 21)
`tarefa` · `problema` · `solucao` · `arquivos` · `comandos` · `ordem` · `dependencias` · `resultado` · `testes` · `contraprova` · `data` · `versao` · `observacoes` · `riscos` · `quando_nao_utilizar` · `procedimento_exato`.

## Classificação
Toda solução registrada é **PROCEDIMENTO_COMPROVADO** — não modificar sem razão técnica documentada.

## Uso
Consultar **ANTES** de inventar uma solução nova. Se já existe solução comprovada para o problema, reutilizar.
